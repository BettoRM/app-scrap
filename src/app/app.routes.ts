import { Routes } from '@angular/router';
import { LoginxComponent} from './components/secure/loginx-component/loginx-component';

export const routes: Routes = [{ path: 'login', component: LoginxComponent }];
